import { MadCurrencyPipe } from './mad-currency.pipe';

describe('MadCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new MadCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
